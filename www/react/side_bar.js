import React from 'react';
import EditorButton from './editor_button'

let SideBar = () => (
  <div id="side-bar">
    <EditorButton text="Wall"  />
    <EditorButton text="Floor" />
  </div>
);

export default SideBar;
